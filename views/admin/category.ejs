<%- include('partials/header.ejs',{tab:'product'}) %>
    <section class="content-main">
        <div class="content-header">
            <div>
                <h2 class="content-title card-title">Categories </h2>
                <p>Add, edit or delete a category</p>
            </div>
            <div>
                <input type="text" placeholder="Search Categories" class="form-control bg-white">
            </div>
        </div>
        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-3">
                        <form id="form" onsubmit=" return  submitForm(event)" action="/admin/create/categoty"
                            method="post">
                            <div class="mb-4">
                                <label for="product_name" class="form-label">Name</label>
                                <input type="text" placeholder="Type here" class="form-control" name="name"
                                    onkeyup="nameValidation()" id="nameId" />
                                <span id="nameValue" class="text-danger"></span>
                                <span  style="color: rgb(228, 20, 20);" > <%= message %></span>
                            </div>


                            <div class="mb-4">
                                <label class="form-label">Description</label>
                                <textarea placeholder="Type here" class="form-control" name="description"
                                    onkeyup="descriptionValidation()" id="descriptionId"></textarea>
                                <span id="descriptionValueError" class="text-danger"></span>
                            </div>
                            <div class="d-grid">
                                <button class="btn btn-primary">Create category</button>
                            </div>
                        </form>
                    </div>
                    <div class="col-md-9">
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th class="text-center">#</th>
                                        
                                        <th>Name</th>
                                        <th>Description</th>
                                        <th>Status</th>
                                        <th>List/Unlist</th>
                                        <th class="text-center">Edit</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% category.forEach((element, index)=> { %>
                                        <tr>
                                            <td class="text-center">
                                                <%= index + 1 %>
                                            </td> <!-- Add a placeholder for the serial number or index -->
                                            <td>
                                                <%= element.name %>
                                            </td>
                                            <td>
                                                <%= element.description %>
                                            </td>
                                            <td>
                                                <% if (element.isblock==true) { %>
                                                    <span class="badge rounded-pill alert-success">Active</span>
                                                    <% } else { %>
                                                        <span class="badge rounded-pill alert-danger">Inactive</span>
                                                        <% } %>
                                            </td>
                                            <td>
                                                <% if (element.isblock==true) { %>
                                                    <button id="userdeleteblock" 
                                                        onclick="ListCategory('<%= element._id %>')"
                                                        class="btn btn-danger">
                                                        <%= (element.isblock===true)?'List':'Unlist'%>
                                                    </button>
                                                    <% } else { %>
                                                        <button id="userdelete"
                                                            onclick="ListCategory('<%= element._id %>')"
                                                            class="btn btn-success">
                                                            <%= (element.isblock===true)?'List':'Unlist'%>
                                                        </button>
                                                        <% } %>
                                            </td>
                                            <td class="text-center">

                                                <a href="/admin/editCategory/<%= element._id %>"><button
                                                        class="btn btn-primary">Edit</button></a>


                                            </td>
                                        </tr>
                                        <% }) %>
                                </tbody>
                            </table>

                        </div>
                    </div> <!-- .col// -->
                </div> <!-- .row // -->
            </div> <!-- card body .// -->
        </div> <!-- card .// -->
    </section> <!-- content-main end// -->
    <footer class="main-footer font-xs">
        <div class="row pb-30 pt-15">
            <div class="col-sm-6">
                <script>
                    document.write(new Date().getFullYear())
                </script> ©, Evara - HTML Ecommerce Template .
            </div>
            <div class="col-sm-6">
                <div class="text-sm-end">
                    All rights reserved
                </div>
            </div>
        </div>
    </footer>
    </main>
    <script src="/asset/js/vendor/jquery-3.6.0.min.js"></script>
    <script src="/asset/js/vendor/bootstrap.bundle.min.js "></script>
    <script src="/asset/js/vendors/select2.min.js"></script>
    <script src="/asset/js/vendors/perfect-scroll"></script>
    <script src="/asset/js/vendors/jquery.fullscreen.min.js"></script>
    <!-- Main Script -->
    <script src="/asset/js/main.js" type="text/javascript"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
    </body>

    </html>

    <script>

        async function ListCategory(id) {

            Swal.fire({
                title: 'Warning',
                text: 'Are you sure',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'OK',
                cancelButtonText: 'Cancel'
            }).then(async (result) => {
                if (result.isConfirmed) {
                    const responds = await fetch('/admin/categoryList?id=' + id);

                    await responds.json()
                    if (responds.status === 200) {
                        block = true
                        block = !block;
                        location.reload()
                    }
                }
            })
        }

    </script>


    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const form = document.getElementById('form');
            const nameValue = document.getElementById('nameId');
            const descriptionValue = document.getElementById('descriptionId');
            const nameValueError = document.getElementById('nameValue');
            const descriptionValueError = document.getElementById('descriptionValueError');

            // Clear initial error messages
            nameValueError.innerText = '';
            descriptionValueError.innerText = '';

            const nameRegex = /^[A-Za-z]+$/;

            function nameValidation() {
                const name = nameValue.value.trim();

                if (!name.match(nameRegex)) {
                    nameValueError.innerText = 'Enter a valid name';
                    return false;
                }

                if (name.length === 0) {
                    nameValueError.innerText = 'Name is required';
                    return false;
                }

                nameValueError.innerText = '';
                return true;
            }

            function descriptionValidation() {
                const description = descriptionValue.value.trim();

                if (description.length === 0) {
                    descriptionValueError.innerText = 'Description is required';
                    return false;
                }

                if(description.length>100){
                     descriptionValueError.innerText='description length must be under 50 letters'
                     return false
                }

                descriptionValueError.innerText = '';
                return true;
            }

            function submitForm(event) {
                event.preventDefault();

                if (!nameValidation() || !descriptionValidation()) {
                    return false;
                } else {
                    form.submit();
                    return true;
                }
            }

            form.addEventListener('submit', submitForm);
            nameValue.addEventListener('keyup', nameValidation);
            descriptionValue.addEventListener('keyup', descriptionValidation);
        });
    </script>